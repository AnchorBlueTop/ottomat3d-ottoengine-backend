export default interface PrintJobRepresentation {
    id?: string,
    name?: string,
    printer?: string,
    duration?: string,
    filament?: string,
    filament_weight?: string,
    filament_length?: string,
    status?: string,
}